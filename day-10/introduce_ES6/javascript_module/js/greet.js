const greet = (name) => {
  return `hello ${name}`;
};

// const mul = (a, b) => {
//   return a * b;
// };

export default greet;

// export default mul

//default export only 1 export
//namely export can multiple export
